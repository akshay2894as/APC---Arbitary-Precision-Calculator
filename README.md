# APC---Arbitary-Precision-Calculator
The Arbitrary Precision Calculator (APC) is a C-based project that performs arithmetic operations on integers of unlimited length using a Doubly Linked List (DLL). Unlike built-in C data types (int, long, long long), this calculator can handle very large numbers without overflow by storing each digit in a separate linked list node.
